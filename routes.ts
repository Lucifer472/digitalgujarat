/**
 * Protected Routes
 */

export const adminRoutes = "/admin";

/**
 * Sing up routes
 */
export const signUpRoutes = ["/auth/login"];

/**
 * Official Redirect Route
 */

export const redirectRoute = "/admin";

/**
 * Api Routes that don't need protection
 */
export const apiRoutePrefix = "/api/auth";
